---
interface Props {
  href?: string;
  title: string;
  description?: string;
  variant?: 'default' | 'featured';
}

const { href, title, description, variant = 'default' } = Astro.props;

const Tag = href ? 'a' : 'div';
const baseClasses = 'block bg-forest-800 border rounded-lg p-6 transition-all duration-200';
const variantClasses = {
  default: 'border-forest-600 hover:bg-forest-700 hover:border-forest-500 hover:shadow-md',
  featured: 'border-bronze-600/50 shadow-sm hover:bg-forest-700 hover:border-bronze-500 hover:shadow-md',
};
---

<Tag
  href={href}
  class:list={[baseClasses, variantClasses[variant], href && 'cursor-pointer']}
>
  <h3 class="font-serif text-lg font-semibold text-bronze-400 mb-2">
    {title}
  </h3>
  {description && (
    <p class="text-forest-200 text-sm leading-relaxed">
      {description}
    </p>
  )}
  <slot />
</Tag>
