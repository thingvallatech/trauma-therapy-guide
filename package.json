{
  "name": "trauma-therapy-guide",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test": "vitest run",
    "verify": "npm run lint --if-present && npm run typecheck --if-present && npm run test --if-present && npm run build && npm run audit:site",
    "typecheck": "tsc --noEmit",
    "test:e2e": "playwright test",
    "audit:site": "node scripts/audit-built-site.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.0",
    "@tailwindcss/vite": "^4.1.17",
    "@types/three": "^0.183.1",
    "astro": "^5.16.4",
    "tailwindcss": "^4.1.17",
    "three": "^0.183.2"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.63.0",
    "vitest": "^3.2.7"
  }
}
